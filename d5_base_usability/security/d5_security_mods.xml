<odoo>
    <record id="base.user_admin" model="res.users">
        <!-- add full accounting features on admin user -->
        <field name="groups_id" eval="[(4,ref('account.group_account_user'))]" />
    </record>
</odoo>
